{
  "name": "firebaseadmin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setCustomClaim": "node index.js",
    "install": "npm install && chmod +x firebaseAdminCLI.sh && chmod +x createSymlink.sh && ./createSymlink.sh && echo \"firebaseAdminCLI was installed succesfully.\" && echo \"Call firebaseAdmin to start the CLI\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "prompt": "^1.3.0"
  }
}
