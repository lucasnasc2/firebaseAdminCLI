{
  "name": "firebaseadmin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "firebaseAdmin": "node index.js",
    "installFirebaseAdmin": "npm install && chmod +x createSymlink.sh && ./createSymlink.sh && echo \"firebaseAdminCLI was installed succesfully.\" && echo \"Call firebaseAdmin to start the CLI\" && echo \"Make sure to create a firebaseCredential.json file with you private service key, on the firebaseAdminCLI folder\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "prompt": "^1.3.0"
  }
}
